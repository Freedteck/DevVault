/** @type {import('next').NextConfig} */
const nextConfig = {
  // Server-side rendering mode (not static export)
  // This enables API routes, server components, and ISR caching
};

export default nextConfig;
